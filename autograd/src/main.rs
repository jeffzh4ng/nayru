use compiler::engine::Expr;
use ordered_float::NotNan;

fn main() {
    // let a = Expr {
    //     evaluated: NotNan::new(2.0).unwrap(),
    //     expr: None,
    // };
    // let b = Expr {
    //     evaluated: NotNan::new(-3.0).unwrap(),
    //     expr: None,
    // };
    // let c = Expr {
    //     evaluated: NotNan::new(10.0).unwrap(),
    //     expr: None,
    // };

    // let d = a * b;
    // let e = d + c;
    // let f = Expr {
    //     evaluated: NotNan::new(-2.0).unwrap(),
    //     expr: None,
    // };
    // let output = e * f;
    // println!("{:?}", output);
}
